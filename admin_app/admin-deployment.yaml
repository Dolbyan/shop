apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-main-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-main-app
  template:
    metadata:
      labels:
        app: admin-main-app
    spec:
      containers:
        - name: admin-main-app
          image: gcr.io/app-integration-435314/admin-app-image:latest
          ports:
          - containerPort: 8000
          command:
          - /bin/sh
          - -c
          - |
            echo "Current directory:"
            pwd
            echo "Directory contents:"
            ls -la
            echo "Python path:"
            python -c "import sys; print(sys.path)"
            echo "Starting gunicorn..."
            gunicorn --bind 0.0.0.0:8000 eshop.wsgi:application
          env:
          - name: PYTHONPATH
            value: "/app/admin"
          - name: DJANGO_SETTINGS_MODULE
            value: "eshop.settings"
          - name: KAFKA_BROKER_URL
            value: "kafka:9092"
          - name: GOOGLE_APPLICATION_CREDENTIALS
            value: "/config/credentials.json"
          volumeMounts:
          - name: gcp-credentials
            mountPath: /config
            readOnly: true
      volumes:
        - name: gcp-credentials
          secret:
            secretName: gcp-credentials
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
spec:
  ports:
    - port: 8000
  selector:
    app: admin-main-app
  type: LoadBalancer